[project]
name = "e-bazaar-django"
version = "1.0.0"
requires-python = ">=3.12,<3.13"

[tool.vercel]
entrypoint = "api.index:app"

[tool.vercel.scripts]
build = "python ECommerce/vercel_build.py"

[tool.setuptools]
packages = []
